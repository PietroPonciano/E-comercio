VITE_API_URL=http://localhost:0000

# Em dev, o checkout usa sandbox_init_point (import.meta.env.DEV).
# Configure no backend as back_urls apontando para:
# /checkout/success, /checkout/failure, /checkout/pending